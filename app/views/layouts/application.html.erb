<!DOCTYPE html>

<html lang="fr">

  <head>

    <!-- Titre de la page (affiché dans les résultats de Google) -->
    <title>WorkSpot - Trouve les meilleurs espaces de travail autour de toi</title>

    <!-- Meta description (affichée sous le titre dans Google) -->
    <meta name="description" content="WorkSpot te permet de découvrir et comparer les meilleurs cafés, bars et lieux pour travailler, avec WiFi, prises et une ambiance agréable.">

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Fav icon -->
    <link rel="icon" href="/images/favicon.png" type="image/png">
    <link rel="apple-touch-icon" href="/images/logo.png">




  </head>

  <body data-controller="anchor-scroll">

    <% if flash[:notice] || flash[:alert] %>
      <div class="toast-flash <%= flash[:alert] ? 'alert-danger' : 'alert-success' %>">
        <%= flash[:notice] || flash[:alert] %>
      </div>
    <% end %>
    <%= render "shared/navbar" %>
    <%= yield %>

  </body>
</html>
