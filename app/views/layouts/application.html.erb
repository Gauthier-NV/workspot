<!DOCTYPE html>
<html lang="fr">
  <head>
    <!-- Titre de la page (affiché dans les résultats de Google) -->
    <title>WorkSpot - Trouve les meilleurs espaces de travail autour de toi</title>

    <!-- Canonical URL -->
    <link rel="canonical" href="https://www.workspots.fr/" />

    <!-- Meta description -->
    <meta name="description" content="WorkSpot te permet de découvrir et comparer les meilleurs cafés, bars et lieux pour travailler, avec WiFi, prises et une ambiance agréable.">

    <!-- Viewport -->
    <meta name="viewport" content="width=device-width,initial-scale=1, viewport-fit=cover">

    <!-- PWA / iOS -->
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#000000">

    <!-- Favicon -->
    <%= favicon_link_tag 'favicon-ws.png' %>

    <!-- Rails security -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Importmap + Styles -->
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Données structurées JSON-LD -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "Workspot",
      "url": "https://www.workspots.fr",
      "description": "Workspot référence les meilleurs cafés et lieux pour télétravailler : wifi, prises, ambiance agréable.",
      "publisher": { "@type": "Organization", "name": "Workspot" }
    }
    </script>

    <!-- Mapbox access token pour ton JS -->
  <meta name="mapbox-token" content="<%= Rails.application.credentials.dig(:mapbox, :access_token) %>">
  <!-- Mapbox CSS -->
<link href="https://api.mapbox.com/mapbox-gl-js/v3.5.1/mapbox-gl.css" rel="stylesheet">

<!-- Mapbox JS -->




  </head>

  <body data-controller="anchor-scroll">
    <% if flash[:notice] || flash[:alert] %>
      <div class="toast-flash <%= flash[:alert] ? 'alert-danger' : 'alert-success' %>">
        <%= flash[:notice] || flash[:alert] %>
      </div>
    <% end %>

    <%= render "shared/navbar" %>
    <%= yield %>
  </body>
</html>
